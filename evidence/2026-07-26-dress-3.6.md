# 3.6 dress rehearsal — verified run log (26 Jul 2026, 04:51–04:52 UTC)

Search `98b85fd9-b626-41c1-ab16-2d8e5b047613` · Creon 25,000 ×2 · CB2 1TN
(fresh Cambridge DEV_TEST set, FAKE11–20) · click → settled **1m36s**, zero pokes.

## Marvin's recollection vs the logs

Claimed: 6 outgoing, 5 picked up. Logs: **6 dials confirmed** (dial_log
connected ×6, six DISTINCT pharmacy numbers, 6 ElevenLabs conversations,
exactly 2 per team phone). The un-picked-up call is `conv_0601…` — **2 seconds
long**: the declined call hit voicemail and the (newly enabled) voicemail
detection ended it immediately, no message. Pre-fix comparison: the morning
slice sat through 119s of voicemail. All six calls produced
`post_call_transcription` webhooks; zero were lost.

## Scenario → board outcome

| Scenario played | Row | Landed as |
|---|---|---|
| Happy path (shelf-check silence) | Sainsburys Pharmacy | **b1 in_stock ×2** ✓ |
| Orderable "Thursday" | Castle Hill Pharmacy | **b2 orderable, eta Thursday** ✓ |
| Plain no | Chesterton Pharmacy | **b3 out_of_stock** ✓ |
| Wrong branch (played twice) | Trumpington St · Regent | **b4 wrong_location, no stock payload** ✓✓ |
| Declined → voicemail | Newnham Chemist | **b4 "unclear"** (honest non-verdict) ✓ |
| Robot check (inside one call) | — | truthful disclosure, call continued ✓ |

Agent behavior fixes verified by ear + logs: hangs up by itself after every
goodbye (end_call tool), voicemail bail-out in 2s.

## Machine gate

`select status, count(*)` for the search: `{verdict: 4, wrong_location: 2,
expired: 4}` — terminal-only, **zero zombies**; search status `complete`,
settled by drain with bench leftovers expired.

## Finding → fix (same hour)

The 3 dead-end calls (2× wrong branch, 1× voicemail) should each have pulled a
bench replacement; none did — bench promotion only fired on
`call_initiation_failure`, not on extraction-decided dead ends. Fixed in
`extract_result` (bucket-4 outcome → promoteBench + dispatch), named test
`bench.extraction-deadend`, 81/81 green, deployed to production before the
09:00 REAL run.
